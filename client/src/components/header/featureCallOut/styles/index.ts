import styled from 'styled-components'
import {HeaderFeatureCallOutType} from '../types'

export const FeatureCallOut: HeaderFeatureCallOutType = styled.h2`
    color: white;
    font-size: 50px;
    line-height: normal;
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.45);
    margin: 0;
`
