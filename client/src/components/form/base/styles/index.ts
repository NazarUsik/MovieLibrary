import styled from 'styled-components'
import {FormBaseType} from '../types'

export const Base: FormBaseType = styled.form`
    display: flex;
    flex-direction: column;
    max-width: 450px;
    width: 100%;
`
