import styled from 'styled-components'
import {ProfilesListType} from '../types'

export const List: ProfilesListType = styled.ul`
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: row;
`
