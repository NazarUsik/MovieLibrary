import styled from 'styled-components'
import {FooterColumnType} from '../types'

export const Column: FooterColumnType = styled.div`
    display: flex;
    flex-direction: column;
    text-align: left;
`
